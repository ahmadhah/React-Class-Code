import React from 'react'

function GrandChild({number}) {
    return (
        <div>
            <h1>Hello itss Grand Child component...........:-)</h1>
            <p>My Count in my Child component : {number}</p>
        </div>
    )
}

export default GrandChild
